# 更新日志

## 主要新增能力

### 1. Arena Direct 动态模型目录与路由

- 新增独立的 Arena 模型目录配置与缓存管理（`config/arena_model_catalog.local.json`、`scripts/arena_models_cache.json`）。
- 支持模型目录规范化、别名与内部 UUID 映射、公开模型 ID 生成、按站点/预设/标签页隔离模型目录，以及 OpenAI `models` 列表合并。
- 支持按 modality、包含/排除关键词和暗池时间窗筛选模型，并避免把离线缓存模型错误暴露到无活动的 Arena Direct 标签页。
- 新增通用目录路由器和模型选择处理器，将 API 路由与 Arena 专属实现解耦；模型切换会校验页面实际显示名称和当前模式。

### 2. Arena 生图生命周期重构

- 将生图判断收敛为独立状态机/观察者：等待页面 hydration、生成中、成功、内容违规、生成失败、空结果等状态均有明确判定。
- 以最新助手消息节点为范围识别图片，结合原生 Stop 按钮、停止覆盖层、生成中文案、spinner/canvas 等信号，避免历史消息或用户上传图触发误判。
- 增加发送前结果基线和节点 token/count 校验，过滤旧错误、旧图片、重复渲染节点和“原图回传”；生成结果支持图片签名/相似度校验。
- Arena 偶发的图片对比翻牌会自动跳过并做防抖；渲染崩溃、Terms of Use 拒绝、空结果等终态会返回明确的不可重试错误（422）。
- 新增通用 `StreamObserver` 扩展点，由 `ArenaImageStreamObserver` 接入流式监听，保留图片 URL、错误和最终 settle 阶段的状态。

### 3. 网络监听、过盾与工作流恢复

- 网络监听支持 Arena 流的可恢复状态（403/429/503，以及 `post-to-evaluation` 404）：在限定窗口内等待后续流重发，超时后再回退 DOM 或抛出终态错误。
- 区分首次响应超时、已有输出后的静默期、后续流等待期，并排除 Arena 的停止控制接口，降低误把控制响应当成正文的概率。
- 新增通用 Cloudflare Turnstile/5 秒盾处理器：可探测 iframe/选择器/坐标、执行低熵鼠标移动与精确点击、确认验证完成，并跳回本次请求实际占用的 URL；Arena 保留兼容适配层。
- Arena 发送看门狗增加附件上传失败检测、发送前页面错误基线和 React 节点替换容错；意外跳转到 Battle/首页时支持自动恢复后重试。
- 工作流新增可注册的错误恢复处理器、模型选择处理器、Transport Profile 处理器、脚本清理回调和动态沙箱导入白名单；JavaScript 步骤支持文件/内联模式、生命周期和脚本预览。

### 4. 请求 API 与错误处理稳定性

- 新增统一错误元数据解析与响应构造，统一处理 Arena 终态错误、上游 HTTP 状态码、手动终止和内部取消，并通过 `x-should-retry` 明确重试语义。
- 请求执行上下文会传递到后台 worker；取消、绝对超时、客户端断开和取消风暴均有结构化收尾，避免把内部取消误报为 500。
- SSE 默认心跳间隔调整为 5 秒；`stream_options.include_usage` 可返回实际估算 token 用量；流式响应保留媒体项并支持 stop 序列过滤。
- Responses API 的会话历史改为有上限、有 TTL 的序列化 LRU 存储，降低包含 base64 图片时的锁竞争和内存复制开销。
- 新增通用响应文本过滤器，清理媒体生成临时占位 URL、规范连续换行并应用 stop 序列。

### 5. 媒体、代理和浏览器链路

- 抽取共享图片校验与签名工具，统一处理上传图读取、远程 URL、data URI、R2 签名 URL 和“生成结果与原图相同”的判断。
- 媒体下载增加代理配置合并、SOCKS5 远程 DNS、失败时的通用 DOM 图片截图回退，并按请求/凭据分区缓存，减少跨请求串图。
- 流监控和图片提取支持图片优先、视频/音频并存、占位图过滤、流式媒体与 DOM 媒体合并，以及网络流中断后的恢复等待。

## 控制面板与配置变化

- 工作流面板新增 Arena 模型目录开关、modality、关键词和暗池配置，并支持按预设自动保存。
- 标签页池支持分配模式切换（优先空闲/轮询/随机）、标签页路由组增删改、组内成员筛选、路由组侧栏折叠/调整宽度/记忆布局、标签页搜索和模型显示名称保存。
- 标签页池新增排除 URL、保留错误标签页、按 URL 自动记忆预设等开关，并显示 `closed` 状态。
- 日志面板新增按请求筛选、技术原文展开、JSON 格式化和复制原文；日志级别和关键操作采用更清晰的颜色标识。
- `app/core/config.py` 改为兼容性门面，配置、浏览器常量、异常、SSE、消息校验、日志收集/脱敏/格式化等职责拆分到 `app/core/config_parts/`，保持原有导入路径兼容。
- `config/browser_config.json` 增加通用生图路由组并启用 URL-预设记忆；模型目录相关设置迁移到独立配置文件，站点配置保存时保留非 Arena 目录内容。

## 工程化与回归覆盖

- 在未被 `.gitignore` 排除的测试中，补充/扩充模型路由、请求生命周期、媒体提取、命令结果竞态、路由组、脚本加载与前端竞态等回归覆盖。
- 命令结果存储拆分为独立 CRUD 层，使用原子写入、记录上限和线程锁；Arena 规则检测、Transport Profile 和结果桥接不再与存储层强耦合。
- 清理旧的 `docs/github-similar-projects-research.md`；保留并更新工作流生命周期研究文档和脚本示例。
